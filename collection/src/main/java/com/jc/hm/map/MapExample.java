package com.jc.hm.map;

/**
 * A map is a collection of key-value pairs where each key is unique.
 * There are 2 interfaces for implementing Map in Java: Map and SortedMap,
 * and 3 classes: HashMap, LinkedHashMap, and TreeMap.
 * A map doesn't allow duplicate keys, but you can have duplicate values.
 * HashMap and LinkedHashMap allow null keys and values,
 * but TreeMap doesn't allow any null key or value.
 */
public class MapExample {
    public static void main(String[] args) {
        System.out.println("Map Example");
    }
}
